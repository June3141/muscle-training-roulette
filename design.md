# トレーニング種目提案サイト — 初期設計ドキュメント

作成日: 2026-08-17 / ステータス: ドラフト

---

## 1. これは何か

部位を選ぶと、その部位をカバーする種目セットを提案する静的 Web サイト。
ユーザーデータを一切保持せず、出力のみを行う。

**本当の目的は、将来のトレーニングアプリのコアとなる 2 つの資産を、
ユーザーもDBも認証も持たない状態で先に完成させること。**

1. 種目 × 筋肉の**重み付きデータセット**
2. 「指定部位を最もよくカバーする N 種目」を選ぶ**選択エンジン**

サイトはこの 2 つを目視検査するための計測器であり、成果物の本体ではない。

### 非目的（重要）

- **需要検証ではない。** 反応の有無から市場を推論しない
- ユーザー獲得ではない。既存アプリ離脱者はここには来ない
- 記録機能・ログイン・保存は作らない

この線を引いておかないと、反応が薄かったときに誤った結論を引く。

---

## 2. 検証したい問い

| # | 問い | 何をもって検証されたとするか |
|---|---|---|
| Q1 | 重み付けデータは「特に効く部位」を表現できるか | 出力セットのカバレッジ図を見て、経験者が違和感を持たない |
| Q2 | 器具・グリップを独立軸として扱えるか | 無効な組み合わせが検出でき、有効なものだけが生成される |
| Q3 | 定番の分割法に近い出力が出るか | ゴールデンセット（§7）と妥当な範囲で一致する |

Q1 が本丸。**カバレッジの可視化がないと Q1 は検証できない**ので、可視化は必須機能。

---

## 3. スコープ

### 作る

- 部位選択 UI（複数選択、部位数は自由）
- 種目数の指定（1〜10）
- 器具フィルタ（任意）
- 種目セットの生成
- **部位カバレッジの可視化（人体図 or バーチャート）**
- 個別種目の差し替え + **差し替え時のカバレッジ差分表示**
- 種目ごとの器具・片手/両手の切り替え

### 作らない

- 記録・保存・ログイン
- 回復度／筋肉痛の考慮（データがないため）
- 分割法プリセット（PPL 等）※ §4.2 の `force` を持っておけば後付け可能
- Watch / ヘルス連携

---

## 4. データ設計

### 4.1 ベースデータ

**free-exercise-db (yuhonas) を採用。** Unlicense のため制約なし。

| 項目 | 内容 |
|---|---|
| 種目数 | 800+ |
| 使うフィールド | `id` `name` `force` `level` `mechanic` `equipment` `primaryMuscles` `secondaryMuscles` `category` |
| 画像 | 使わない（後述） |

採用理由は種目数ではなく **`force`（push/pull）と `mechanic`（compound/isolation）を持っていること。**
前者は分割法プリセットの後付けに、後者は重みのベースライン生成に必須。

**検討して不採用にしたもの**

- `hasaneyldrm/exercises-dataset` — 1,324種目と多いが、`force`/`mechanic` が無い。
  メディアは Gym visual 著作物で再利用に別途ライセンスが必要。三角筋が `delts` の一塊で粒度不足。
  → **散文の解説文のみ、重み推定時の LLM 入力として参照**（MIT）
- `wger` — 日本語名の参考にはなるが CC-BY-SA の継承条件があるためデータは混ぜない
- ExerciseDB 系各種 — メディアが商用ライセンス

### 4.2 拡張スキーマ

```jsonc
{
  "id": "barbell_bench_press",
  "nameEn": "Barbell Bench Press",
  "nameJa": "バーベルベンチプレス",        // 自前
  "force": "push",                        // 分割法プリセット用に保持
  "mechanic": "compound",
  "movementPattern": "horizontal_press",   // 自前・多様性制約用

  // 器具を独立軸に分離（ここが本命の検証項目）
  "equipmentOptions": ["barbell", "dumbbell", "smith", "machine"],
  "defaultEquipment": "barbell",
  "lateralityOptions": ["bilateral"],      // bilateral | unilateral
  "defaultLaterality": "bilateral",

  // 重み: 合計 1.0 に正規化
  "muscleWeights": {
    "pectoralis_major_sternal": 0.45,
    "pectoralis_major_clavicular": 0.15,
    "triceps": 0.25,
    "deltoid_anterior": 0.15
  }
}
```

### 4.3 筋肉タキソノミー

既存データセットはどれも粒度が粗い（`delts` / `chest` 等の一塊）。
**部位可視化を成立させるには最低限この分割が必要。**

- 三角筋 → 前部 / 中部 / 後部
- 大胸筋 → 上部（鎖骨部）/ 中下部（胸肋部）
- 背中 → 広背筋 / 僧帽筋上部 / 僧帽筋中下部 / 菱形筋 / 脊柱起立筋
- 上腕 → 二頭 / 三頭 / 腕橈骨筋
- 下半身 → 大腿四頭 / ハム / 大臀 / 中臀 / 内転筋 / 下腿三頭
- 体幹 → 腹直筋 / 腹斜筋 / 腹横筋

「Core」のような部位総称は筋肉名として使わない。

> **⚠ 可視化 SVG 側の粒度と衝突する**
> 既存の body-highlighter 系 SVG（§8.2）は `chest` / `front-deltoids` / `back-deltoids` 程度の粒度で、
> **三角筋中部と大胸筋の上下分割を持たない。**
> 上記タキソナミーを採るなら SVG のパスを自分で分割する必要がある。M1 で判断する。
> 分割しない場合、サイドレイズとショルダープレスが同じ場所を光らせることになり、
> Q1（特に効く部位を表現できるか）の検証力が落ちる。

### 4.4 重みの生成手順

1. **決定論ベースライン** — `mechanic` と primary/secondary の要素数から機械的に按分
   - `isolation` → primary に 0.75〜0.85、残りを secondary に分配
   - `compound` → primary 間で分配、secondary は 0.10〜0.20 帯
2. **粒度の展開** — `chest` → 上部/中下部 等へ、種目名と instructions から推定
3. **LLM レビュー** — ベースラインと解説文を渡し、差分と理由を出力させる
4. **人力確定** — 差分のみ確認。全件を手で振らない

EMG（%MVIC）は使わない。単位が「その筋自身の最大収縮に対する割合」であり
筋間で比較できず、合計が 1 にならないため、必要な配分量に変換できない。

### 4.5 未決定事項（実装前に決める）

> **⚠ 片手/両手の重量正規化ルール**
> ダンベル片手20kg は `20` か `40` か。負荷を「片側」で持つか「合計」で持つかは
> スキーマの根幹であり、後から変えると全データを書き直すことになる。
> **推奨: 片側で保持し、表示時に合計へ変換。** ただし要確認。

> **⚠ 器具変更時に重みは変わるか**
> ダンベルベンチはバーベルベンチより安定性要求が高く、三角筋前部の比率が上がる。
> v1 では「変わらない」として単純化し、必要なら `equipmentModifiers` を後付けする。

---

## 5. 選択エンジン

### 5.1 アルゴリズム

「指定部位を最もよくカバーする N 種目」は**重み付き最大被覆問題**。
全探索は不可（800 から 5 種目で約 2.7 兆通り）だが、貪欲法で近似できる。
最大被覆の貪欲法には (1 − 1/e) ≒ 63% の近似保証がある。実装は数十行。

**アルゴリズムは検証項目ではない。検証されるのは重み付けデータの方。**

### 5.2 目的関数は 1 つではない

単純な被覆最大化だけだと、胸 5 種目が全部水平プレス系になる。
以下を重み付き和として扱う（係数は調整対象）。

| 項 | 内容 |
|---|---|
| カバレッジ | 選択部位の重み合計を、筋ごとに凹関数を噛ませて最大化 |
| 多様性 | distinct な `movementPattern` 数への加点 |
| 複合種目優先 | 序盤に `compound` を配置しやすくする |

**重複ペナルティは独立した項にしない。**
カバレッジ項の凹関数が同一筋への集中を自動的に減点するので、別に足すと二重に掛かる。
多様性を減点ではなく加点で書くのも、そうしないと単調劣モジュラ性が崩れるため
（[ADR 0008](./docs/adr/0008-selection-objective-and-algorithm.md)）。

### 5.3 順序付け

被覆問題は「集合」しか出さないが、実際に必要なのは「列」。
貪欲法で集合を作った後、ルールベースで並べ替える。

1. compound → isolation
2. 大筋群 → 小筋群
3. **先に小筋群を潰さない**（三頭を先に潰すとベンチが挙がらない）

この順序ルールは既存アプリと差がつきうる部分でもある。

### 5.4 想定されるエッジケース

- **候補の枯渇** — 器具フィルタで候補が足りなくなる。
  自重のみだと二頭の種目が事実上存在しない等。Fitbod のドキュメントでも同じ問題が明記されている
- **部位数 > 種目数** — 3部位で2種目を要求されたら、カバーしきれない旨を明示する
- **1部位に10種目** — そのまま出す。警告は出さない（ユーザーの選択）
- **候補プールの偏り** — 元データは腕の種目が胸より多いなど分布が歪んでいる。
  貪欲法にそのまま漏れるので、部位ごとの候補数を確認しておく

---

## 6. UI フロー

```
[1] 部位を選ぶ（複数可・部位数自由）
      ↓
[2] 種目数を選ぶ（1〜10）／ 器具フィルタ（任意）
      ↓
[3] 生成
      ↓
[4] 結果表示
      ├─ 種目リスト（順序付き）
      ├─ カバレッジ可視化 ← 検証の主計測器
      └─ 各種目: 器具・片手/両手の切り替え、差し替え
             ↓
      [5] 変更のたびにカバレッジ差分を表示
          例: 「フライ → ディップスに変更 / 三頭 +0.18, 大胸筋上部 −0.05」
```

分割法（PPL 等）は名乗らない。「部位数も選べる」形にすることで、
出力が特定の分割法として正しいかの責任を負わずに済む。

---

## 7. ゴールデンセット（回帰テスト）

重みを触るたびに既存の妥当な出力が壊れていないか検出する。**これがないと 800 件の調整は破綻する。**

| ケース | 入力 | 期待 |
|---|---|---|
| PPL Push | 胸・三角前部・三頭 / 6種目 | 水平プレス1、インクライン1、肩1、三頭1〜2を含む |
| PPL Pull | 広背・僧帽・二頭 / 6種目 | 垂直プル1、水平プル1、二頭1〜2を含む |
| 下半身 | 四頭・ハム・臀 / 5種目 | スクワット系1、ヒンジ系1、単関節1以上 |
| 胸のみ | 胸 / 5種目 | プレス系とフライ系が両方含まれる |
| 自重のみ | 全身 / 6種目 | 生成に成功する（枯渇しない） |

完全一致は求めない。人間が judge した結果をスナップショットとして保存し、差分だけ確認する。

---

## 8. 技術構成

| 層 | 選定 | 理由 |
|---|---|---|
| 形態 | 完全静的 SPA | ユーザーデータなし = サーバー不要 |
| フレームワーク | Vite + React（または Astro） | 好みで可。ビルド成果物が静的であることのみ必須 |
| データ | ビルド時に JSON をバンドル | 800件なら数百KB。API 不要 |
| 可視化 | SVG の人体図に筋肉パスを持たせ、重みで塗り分け | 画像素材のライセンス問題を回避できる |
| リポジトリ | データとサイトを同一リポジトリに | `/data` と `/web`。サイトはデータのデモを兼ねる |

**画像は使わない。** GIF や写真は軒並みライセンスが問題になる。
SVG の人体図を自前で持てば、可視化にもそのまま使えて一石二鳥。

### 8.1 UI デザインの参考（M6 で検討）

**[Nutlope/hallmark](https://github.com/Nutlope/hallmark)** — Claude Code / Cursor 向けの
anti-AI-slop デザインスキル。MIT。`npx skills add nutlope/hallmark` で導入。

適用するとしたら以下の範囲。**ツール画面本体ではなく、その周辺。**

- `hallmark audit <target>` — 完成した UI を anti-pattern に対して採点し、punch list を出す。
  コードを書き換えない非破壊オペレーションなので、最後に一度回すだけでよい
- データセットの説明セクション（ランディング的な部分）の構成・タイポグラフィ
- ハンドビルド SVG をライブラリより優先する方針が、本プロジェクトの
  「画像素材を使わず SVG を自前で持つ」判断と一致している

**適用しない範囲**

- 部位選択 → 種目リスト → カバレッジ図 のツール画面。
  hallmark はランディングページ寄り（hero / nav / footer archetype、SaaS ページ順序）で、
  アプリケーション UI には効きにくい
- **人体図の重み塗り分け** — ここは固有のデータ可視化問題であり、
  どのデザインスキルも助けにならない。本プロジェクトで唯一の本質的なデザイン作業

注意: Anthropic の `frontend-design` スキルと役割が重なる。同時に有効化すると
指示が競合しうるので、プロジェクト単位でどちらかに寄せる。

### 8.2 人体図 SVG（本命・M1 で評価）

自前で描く前提だったが、**MIT の既製 SVG が一系統ある。**
いずれも `HichamELBSI/react-native-body-highlighter` の SVG パスを祖先に持つ。

| repo | 形態 | 備考 |
|---|---|---|
| **`lahaxearnaud/body-highlighter`** | フレームワーク非依存・依存ゼロ | 第一候補。vanilla/React/Vue/Svelte/Astro で動く。TS5。単一 SVG + ヘルパー |
| `giavinh79/react-body-highlighter` | React | MIT。`.rbh` クラス経由で CSS を直接上書き可能 |
| `soroojshehryar/react-muscle-highlighter` | React | 前面/背面、強度のグラデーション、ARIA + キーボード操作 |
| `HichamELBSI/react-native-body-highlighter` | React Native | 系統の起点。男女・前後・左右の出し分けあり |

**採用方針**

ライブラリとして使わなくても、**SVG パスだけ抽出して自前アセットにできる**（MIT）。
「画像素材を使わず SVG を自前で持つ」という §8 の判断と矛盾しない。ゼロから描く必要が消える。

**2 つのギャップ**

1. **粒度不足** — §4.3 参照。三角筋中部・大胸筋上下がない。パスの分割が必要になる可能性
2. **強度モデルの違い** — これらは離散的な intensity（1 / 2 / 3 や頻度インデックス）を前提としている。
   本プロジェクトの重みは連続値（0〜1）なので、バケット化するか fill を直接制御する。
   `lahaxearnaud` 版は DOM に直接描画するため上書きが最も容易

---

## 9. デプロイ

### 採用: Cloudflare Pages（無料）

| 項目 | 無料枠 |
|---|---|
| 帯域 | 従量制限なし（フェアユース） |
| ビルド | 500回/月、タイムアウト20分 |
| カスタムドメイン | プロジェクトあたり100 |
| ファイル数 | 20,000（1ファイル最大 25 MiB） |

完全静的サイトなのでこの枠に一切触れない。日本国内にも PoP があり応答も速い。
GitHub リポジトリと連携して push で自動デプロイ。

### 代替案

- **GitHub Pages** — データセットのリポジトリをそのまま公開できる最短経路。
  ただし帯域が月100GB程度、CDN が弱い
- **Vercel Hobby** — 商用利用が規約で禁止されている点に注意。
  将来プロダクト化するなら最初から避けた方が面倒がない
- **Netlify** — 帯域100GB/月、ビルド300分/月。悪くないが Cloudflare の上位互換にはならない

### 費用

独自ドメインを取らなければ **完全に 0 円。**
`*.pages.dev` のサブドメインで動く。ドメインは検証が終わってからで十分。

---

## 10. マイルストーン

| # | 内容 | 完了条件 |
|---|---|---|
| M0 | §4.5 の未決定事項を決める | 正規化ルールが文書化されている |
| M1 | 筋肉タキソノミー確定 + free-exercise-db のマッピング | 全種目が新タキソノミーに写像されている |
| M2 | 決定論ベースラインで重み生成 | 全件に `muscleWeights` が入っている（品質は問わない） |
| M3 | 選択エンジン（貪欲法のみ）+ CLI | ゴールデンセットが動く |
| M4 | カバレッジ可視化 | 出力を見て違和感の有無を言語化できる |
| M5 | 重みの人力レビュー | ゴールデンセット全ケースが妥当と判断できる |
| M6 | UI 完成・デプロイ | 公開 URL がある |

M3 まではフロントエンドを書かない。**CLI とゴールデンセットで先に中身を固める方が速い。**

---

## 11. 成果物としての位置づけ

サイトより **データセットの方が資産価値が高い。**

- 日本語名がついた種目データセットは現存しない
- 重み付き（連続値）のものも現存しない
- 器具軸が分離されたものも現存しない

free-exercise-db が Unlicense なので、派生も Unlicense / CC0 で公開できる。
サイトはそのデータのブラウザ兼バグ報告 UI として置くと、
「ランダム種目サイト」より届く相手が明確になる。

なお重み付けには主観が入るため、クラウドソースしても正解に収束する保証はない。
**収束ではなく「明らかな誤りの除去」を目的に据える**こと。
