# コントリビューションガイド

## このプロジェクトが受け取りたいもの

最も価値がある貢献は **重みデータの明らかな誤りの指摘** です。

重み付けには主観が入るため、クラウドソースしても正解に収束する保証はありません。
このプロジェクトは**収束ではなく「明らかな誤りの除去」を目的に据えています**。

- 「サイドレイズに広背筋が入っている」→ 明らかな誤り。歓迎します
- 「ベンチプレスの三頭は 0.25 ではなく 0.22 が妥当」→ 好みの差。判断できないので保留になります

## 受け取れないもの

- **EMG（%MVIC）の数値を根拠とする提案。** 単位が「その筋自身の最大収縮に対する割合」であり、
  筋間で比較できず、合計が 1 にならないため、必要な配分量に変換できません
- **記録・保存・ログイン機能の提案。** スコープ外です（design.md §3）
- **画像・GIF アセットの追加。** ライセンスが問題になるため、可視化は自前の SVG のみで行います（§8）
- **CC-BY-SA 由来のデータ**（wger 等）。継承条件が付くため、Unlicense で再配布できなくなります

## 開発環境

```bash
corepack enable pnpm
pnpm install
pnpm verify        # lint + typecheck + test + 複雑度チェックを一括実行
```

Node.js は `.node-version` のバージョンに合わせてください。

## ゴールデンセット

`packages/engine/test/golden/` に、代表的な入力に対する出力のスナップショットがあります。

**重みを 1 件でも触ると、ここに差分が出る可能性があります。** これは意図された仕組みです。
差分が出た場合、それが改善なのか劣化なのかを人間が判断し、PR の本文に理由を書いてください。
理由なしにスナップショットを更新する PR はマージしません。800 件の調整はこれがないと破綻します。

```bash
pnpm test --project engine       # 差分を確認
pnpm test --project engine -u    # 判断した上で更新
```

いずれもリポジトリのルートで実行してください。
パッケージのディレクトリに移動して `vitest` を直接叩くと、
プロジェクトの root が二重に解決されてテストが 0 件になります。

## ブランチ

| ブランチ | 役割 |
|---|---|
| `main` | リリース可能な状態。`develop` からのみマージする |
| `develop` | 開発の集約先。**feature ブランチはここに squash merge する** |
| `feature/*` | 作業ブランチ。`develop` から切って `develop` に戻す |

feature ブランチの名前は `feature/<Issue番号>-<短い説明>` にします（例: `feature/12-greedy-selection`）。

**squash merge を使うのは、feature ブランチ内の試行錯誤を `develop` の履歴に残さないためです。**
特に重みデータの調整は「生成 → ゴールデンセットの差分を見る → 係数を戻す」を繰り返すので、
そのまま merge commit で入れると履歴から意味を読み取れなくなります。

squash 後のコミットメッセージが実質的な変更単位になるので、
PR のタイトルと本文をそのまま使えるように書いてください。

## コミット

Conventional Commits に従います。

| prefix   | 用途                   |
| -------- | ---------------------- |
| `feat:`  | 機能追加               |
| `fix:`   | バグ修正               |
| `data:`  | 種目データ・重みの変更 |
| `docs:`  | ドキュメント           |
| `test:`  | テストのみ             |
| `chore:` | ビルド・CI・依存       |

`data:` を独立させているのは、データ変更だけを追跡してレビューしたいためです。

## 開発の進め方

テスト駆動で進めます。

1. 期待される入出力に基づいてテストを書く
2. 実行して失敗を確認する
3. 失敗するテストの状態でコミットする
4. テストを通す実装を書く。**実装中はテストを変更しない**
