/**
 * 重量の保持単位（ADR 0001）。
 *
 * v1 に重量入力の UI はないが、`lateralityOptions` を持つスキーマが
 * 「片側の値」を意味するのか「合計」を意味するのかを未定義のまま 800 件に書き込むと、
 * **後から全件の意味が変わる。** 定数として明文化しておく。
 */

/**
 * 器具 1 個あたりの重量を保持する。
 *
 * ダンベル片手 20kg は `20`、バーベル 20kg も `20`。
 * 「片側」と言うとバーベルに片側が存在しないので例外が出るが、
 * 「器具 1 個あたり」なら例外がない。ダンベルは 2 個使うので 1 個あたり、
 * バーベルは 1 個なので全体。
 *
 * 合計への変換は表示層の責務。変換関数は重量入力 UI を作るときに置く
 * （今書いても呼び手がなく、仕様が固まらない）。
 *
 * ケーブル / マシンのスタックは「ピン位置」でこの規則に乗らない。
 * v1 では扱わないが、種目ごとに上書きする余地は残してある（ADR 0001）。
 */
export const LOAD_UNIT = "per_implement";

export type LoadUnit = typeof LOAD_UNIT;
