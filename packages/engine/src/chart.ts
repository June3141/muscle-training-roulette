import type { MuscleId } from "@mtr/data";
import type { Coverage } from "./types.ts";

export function renderCoverageChart(_coverage: Coverage, _targets: readonly MuscleId[]): string {
  throw new Error("未実装");
}
